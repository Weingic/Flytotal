# 2026-04-12 周报（4.12 固化版）

## 第一部分：本周真正完成了什么
1. 完成了单节点 `Node A` 主链、风险链、事件链、视觉增强链、网页展示链的联动打通。
2. 完成了事件对象留存，事件不再只是串口瞬时输出，而是可以在本地 JSON / 事件存储中保留。
3. 完成了视觉抓拍接入，视觉链可在手动选框后实现目标锁定与抓拍。
4. 完成了网页端事件详情展示，能够显示事件号、时间、风险、节点、坐标、RID 和抓拍图。
5. 完成了事件证据导出与最小回放能力，导出 JSON、导出列表、导出详情接口均已打通。
6. 完成了验收工具链标准化，包括：
   - `preflight` 环境预检
   - `startup_helper` 启动助手
   - `acceptance_flow` 快检 / 全检
   - `acceptance_auto` 自动验收
7. 在 `2026-04-12` 真机联调中，已经拿到“真目标 + 真事件 + 真抓拍 + 真网页挂接”的闭环证据。

---

## 第二部分：当前系统已经稳定的边界
1. 单节点 `Node A` 主链可以稳定输出状态并进入事件链。
2. 网页端可以稳定读取主链和视觉链数据并实时展示。
3. 事件可以在本地保留，并与抓拍证据通过 `event_id` 进行精确绑定。
4. 自动验收工具链可以用于日常守门，快速判断当前链路是否可演示。
5. 当前版本已经具备一条可复现的单节点演示流程，可作为 `Demo V1.0` 冻结版本。
6. 当前网页端已经不只是状态看板，而是已经承担“事件留痕 + 证据展示 + 最小回放”的角色。

---

## 第三部分：当前系统还没做 / 没做稳的部分
1. 当前仍然是单节点闭环，双节点协同尚未纳入本周冻结范围。
2. 视觉模块仍以人工选框为主，自动检测与自动起锁尚未落地。
3. 网页端当前是最小回放版本，完整历史时间轴能力尚未建设。
4. 长时间无人值守连续稳定性还没有形成系统化压测结论。
5. 现场设备占用问题（串口、摄像头）仍会影响一次性通过率，还需要继续流程化治理。
6. 身份链 / 白名单链增强尚未并入本周冻结版本。

---

## 第四部分：下周进入的方向
下周方向固定为：
1. 身份链 / 白名单链增强
2. 双节点协同
3. 视觉模块进一步提升
4. 网页端事件回放增强

---

## 本周冻结结论
冻结版本：`Node A Hunter+Vision+Web Demo V1.0`

本周结束时，当前系统已经明确拥有：
1. 一条可复现演示流程
2. 一版稳定参数口径
3. 一套统一字段
4. 一套网页端实时展示
5. 一条最小视觉融合链
6. 一组事件日志与抓拍证据
7. 一份周报
8. 一个正式冻结的可演示版本

---

## 本次真机成功补充记录
本次 `2026-04-12` 固化日已确认：
1. 雷达主链进入 `TRACKING`
2. 风险状态成功提升并创建事件
3. 视觉链完成手动选框后的锁定与抓拍
4. 抓拍图成功按 `event_id` 精确挂接到事件
5. 网页详情页成功显示真实抓拍图、风险等级、RID 状态和坐标
6. 自动验收链曾输出：`quick_result=PASS`、`full_result=PASS`、`result=PASS`

本次真机成功样例事件：`A1-0000002622-0001`
