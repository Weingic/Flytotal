# 低空节点与云端统丢�接口草案

日期：`2026-04-01`

本文档用于定义后�?`A1 / A2 / 云端` 共用的接口字段骨架，作为双节点联调��云端对接和后续协议扩展的统丢�命名基础�?
说明�?- 当前 `A1` 已实现字段，�?[9NodeA上行字段表文�?md](C:/Users/WZwai/Documents/PlatformIO/Projects/Flytotal/docs/9NodeA正式字段表文�?md) 为准�?- 本文档不是替代当�?`A1` 文档，��是在其基础上提炼出跨节点与云端的统丢�字段�?- 本文档允许包含��当前已实现”和“后续预留��两类字段��?
## 丢�、接口目�?
这份统一接口草案要解决的问题是：

1. `A1`、`A2`、云端后续不能各自使用不同命�?2. 当前串口协议、未�?MQTT、未来云�?JSON 应尽量沿用同丢�套字段名
3. 后续扩展事件、轨迹接力��节点协同，不要重新定义丢�套接�?
## 二��统丢�接口分层

后续统一接口建议分成 4 层：

1. 节点层字�?2. 轨迹层字�?3. 状��层字段
4. 事件层字�?
## 三��节点层统一字段

这些字段建议扢�有节点和云端统一保留�?
| 字段�?| 含义 | 当前状��?|
| --- | --- | --- |
| `node` | 节点编号，如 `A1`、`A2` | 已实�?|
| `zone` | 节点扢�属区�?扇区 | 已实�?|
| `role` | 节点角色，如 `EDGE` | 已实�?|
| `ts` | 本地运行毫秒时间�?| 已实�?|

说明�?- `node` 必须是所有帧的基硢�字段
- `zone` 当前建议作为固定节点属��随帧输�?- `role` 目前 `A1` 已实现，后续 `A2` 也建议保持一�?- `ts` 统一表示节点本地时钟，不在节点侧强制做云端时间同�?
## 四��轨迹层统一字段

这些字段建议扢�有轨迹相关帧统一使用�?
| 字段�?| 含义 | 当前状��?|
| --- | --- | --- |
| `track` | 轨迹编号 | 已实�?|
| `active` | 轨迹是否活跃 | 已实�?|
| `confirmed` | 轨迹是否确认 | 已实�?|
| `x` | 横向坐标，单�?mm | 已实�?|
| `y` | 距离/纵向坐标，单�?mm | 已实�?|
| `vx` | 横向速度，单�?mm/s | 已实�?|
| `vy` | 纵向速度，单�?mm/s | 已实�?|
| `seen` | 连续观测计数 | 已实�?|
| `lost` | 丢失计数 | 已实�?|

说明�?- `A1`、`A2` 后续如果都使用轨迹输出，必须保持这组字段丢��?- 云端历史轨迹表建议也沿用这组命名

## 五��状态层统一字段

这些字段建议用于描述节点对目标的研判状����?
| 字段�?| 含义 | 当前状��?|
| --- | --- | --- |
| `hunter` | 风险研判状��?| 已实�?|
| `gimbal` | 云台状��?| 已实�?|
| `rid` | RID 状��?| 已实�?|
| `risk` | 风险分数 | 已实�?|

说明�?- `hunter` 描述业务层判定结�?- `gimbal` 描述执行控制层状�?- `rid` 描述身份核验结果
- `risk` 用于统一风险量化表达

## 六��动作层统一字段

这些字段建议统一表示“当前是否触发某类动作����?
| 字段�?| 含义 | 当前状��?|
| --- | --- | --- |
| `alert` | 是否触发告警 | 已实�?|
| `capture` | 是否触发抓拍 | 已实�?|
| `guardian` | 是否触发合作终端保障链路 | 已实�?|

说明�?- 这组字段未来很��合直接映射到云端事件卡片或工单状��?
## 七��事件层统一字段

这些字段建议用于事件帧��事件表和工单链路��?
| 字段�?| 含义 | 当前状��?|
| --- | --- | --- |
| `reason` | 事件触发原因 | 已实�?|
| `event_id` | 事件编号 | 已实�?|
| `event_level` | 事件等级 | 已实�?|
| `event_status` | 当前处置状��?| 已实�?|

说明�?- 当前 `A1` 已经具备基础 `event_id`
- 当前 `A1` 已补�?`event_level` �?`event_status`，后续云端和多节点建议沿用相同命�?
## 八��节点协同层预留字段

这些字段当前未实现，但建议未�?A1 / A2 / 云端统一时使用��?
| 字段�?| 含义 | 当前状��?|
| --- | --- | --- |
| `zone` | 扢�属区�?扇区 | 已实�?|
| `source_node` | 事件朢�初来源节�?| 预留 |
| `handover_from` | 轨迹接力来源节点 | 预留 |
| `handover_to` | 轨迹接力目标节点 | 预留 |
| `cluster_id` | 多节点协同编�?| 预留 |

说明�?- 这些字段主要服务后续 `A1 / A2` 接力展示和区域联�?
## 九��节点编号规�?
这一节用来先把��节点名字��么取��固定下来，避免后面 A1、A2、云端��上位机各自叫法不同�?
当前建议规则�?
1. 当前固定监测节点统一使用 `A + 数字` 形式  
例如�?- `A1`
- `A2`
- 后续可扩展为 `A3`、`A4`

2. `node` 是节点唯丢�身份，不因重启��重新上电��重新部署脚本��变�? 
例如�?- 某个物理节点既然定为 `A1`，日志��事件��接力字段中都持续使�?`A1`

3. 不再混用其他别名  
例如�?- 不要同时出现 `A-1`
- 不要同时出现 `node1`
- 不要同时出现 `edge01`

4. 扢�有��来源节点����接力来源节点����接力目标节点��字段都必须复用同一套节点编�? 
例如�?- `source_node=A1`
- `handover_from=A1`
- `handover_to=A2`

当前阶段建议�?- `A1` 表示当前已落地的 Node A 主节�?- `A2` 表示后续第二个边缘节�?
## 十��节点角色规�?
这一节用来区分��节点是谁��和“节点负责什么����?
箢�单理解：
- `node` 回答的是“这是哪丢�个节点��?- `role` 回答的是“这个节点承担什么职责��?
建议角色表如下：

| `role` | 含义 | 使用建议 | 当前状��?|
| --- | --- | --- | --- |
| `EDGE` | 前端感知与执行节�?| 负责发现目标、输出轨迹��输出事件��执行本地联�?| 已实�?|
| `CLOUD` | 云端汇聚与处置节�?| 负责汇��多节点数据、展示��分发��留�?| 预留 |
| `RELAY` | 中继/汇聚节点 | 负责转发或区域汇聚，不作为主研判节点 | 预留 |

当前阶段建议�?- `A1`、未�?`A2` 都继续使�?`role=EDGE`
- 云端平台后续如接入，可统丢�使用 `role=CLOUD`

这样做的好处是：
- �?`node=A1`，知道是哪个点位
- �?`role=EDGE`，知道它是边缘感知节�?- 后续即使 `A2`、`A3` 都存在，也不会把“编号��和“职责��混在一�?
## 十一、事件编号与协同编号规则

这一节用来解决��同丢�件事怎么有统丢�编号”的问题�?
要先区分两个概念�?
1. `track`
- 表示节点本地当前轨迹�?- 它可能因为目标丢失��重建��切换节点��变�?- 它��合做本地跟踪，不��合单独拿来做跨节点事件主编�?
2. `event_id`
- 表示系统对某丢�次事件的统一编号
- 适合�?A1、A2、云端共同引�?- 丢�旦生成，后续接力和处置都尽量沿用同一�?`event_id`

建议�?`event_id` 格式�?
```text
<source_node>-<yyyymmdd>-<seq4>
```

示例�?
```text
A1-20260402-0001
```

含义拆解�?- `A1`：这次事件最初由 A1 发起
- `20260402`：事件编号日�?- `0001`：A1 在当天生成的�?1 个事�?
建议规则�?
1. `event_id` 丢�旦生成，后续节点接力时继续沿用，不重新编�?2. `source_node` 固定记录朢�早发现或朢�早立项的节点
3. `track` 可以变，�?`event_id` 尽量不变

对于多节点协同编号，建议 `cluster_id` 使用下面格式�?
```text
CL-<source_node>-<yyyymmdd>-<seq2>
```

示例�?
```text
CL-A1-20260402-01
```

说明�?- `cluster_id` 用于标识“一组协同处置链路��?- 如果当前阶段还不霢�要单独区分协同链，可先仅预留，不强制实现

## 十二、多节点接力朢�小字�?
这一节不是在设计复杂协议，��是先规定��如�?A1 以后要把目标交给 A2，最少得带哪些东西����?
建议朢�小接力字段如下：

| 字段�?| 必要�?| 作用 |
| --- | --- | --- |
| `event_id` | 必需 | �?A1、A2、云端知道这是同丢�件事 |
| `source_node` | 必需 | 标明朢�早来源节�?|
| `handover_from` | 必需 | 标明当前由谁交出 |
| `handover_to` | 必需 | 标明准备交给�?|
| `ts` | 必需 | 标明接力发生时间 |
| `track` | 建议 | 保留交接时发送方本地轨迹�?|
| `x` | 建议 | 交接时横向位�?|
| `y` | 建议 | 交接时纵�?距离位置 |
| `vx` | 建议 | 交接时横向��度估计 |
| `vy` | 建议 | 交接时纵向��度估计 |
| `hunter` | 建议 | 交接时风险研判状�?|
| `rid` | 建议 | 交接�?RID 状��?|
| `risk` | 建议 | 交接时风险分�?|

朢�小理解方式：
- `event_id` 解决“是不是同一件事�?- `handover_from / handover_to` 解决“谁交给谁��?- `x / y / vx / vy` 解决“目标大概在哪��往哪走�?
## 十三、最小接力示�?
下面给一个尽量简单的示例，帮助后续写 A2 或云端时统一理解�?
### 1. A1 首先发现目标

```text
node=A1,role=EDGE,ts=102340,event_id=A1-20260402-0001,source_node=A1,track=3,hunter=SUSPICIOUS,rid=MISSING,risk=63.0,x=320.0,y=1800.0
```

说明�?- 这是 A1 首次形成可对外引用的事件
- 从这丢�刻开始，后续尽量沿用 `event_id=A1-20260402-0001`

### 2. A1 �?A2 发起接力

```text
node=A1,ts=104210,event_id=A1-20260402-0001,source_node=A1,handover_from=A1,handover_to=A2,track=3,x=360.0,y=1750.0,vx=40.0,vy=-10.0,hunter=SUSPICIOUS,rid=MISSING,risk=66.0
```

说明�?- A1 仍然是当前发送节�?- `handover_to=A2` 表示下一跳目标节点是 A2
- `event_id` 保持不变

### 3. A2 接力后继续沿用同丢�事件�?
```text
node=A2,role=EDGE,ts=105020,event_id=A1-20260402-0001,source_node=A1,handover_from=A1,track=8,hunter=SUSPICIOUS,rid=MISSING,risk=64.0,x=410.0,y=1680.0
```

说明�?- A2 的本�?`track` 可以变成新的值，例如 `8`
- �?`event_id` 仍然保持�?`A1-20260402-0001`
- 这样云端就能知道 A1 �?A2 说的是同丢�个目标事�?
## 十四、当前建议的统一命名原则

后续扢�有节点��上位机、MQTT、云端接口建议遵守以下原则：

1. 同一概念只保留一个字段名  
例如�?- 不要丢�处叫 `node_id`，另丢�处叫 `device_id`
- 当前统一�?`node`

2. 坐标字段保持短名  
例如�?- 统一使用 `x / y / vx / vy`
- 不要再出�?`pos_x`、`target_x_mm`、`coord_x` 多套命名

3. 状��字段统丢�用业务名  
例如�?- `hunter`
- `gimbal`
- `rid`

4. 布尔动作统一使用 `0/1` 表达  
例如�?- `alert=1`
- `capture=0`

## 十五、建议的帧骨�?
### 1. 心跳骨架

```text
node, role, ts, hunter, gimbal, rid, risk, alert, capture, guardian
```

### 2. 轨迹骨架

```text
node, ts, track, active, confirmed, x, y, vx, vy, seen, lost
```

### 3. 事件骨架

```text
node, ts, reason, track, hunter, gimbal, rid, risk, alert, capture, guardian, x, y
```

### 4. 后续云端扩展骨架

```text
node, role, ts, event_id, track, hunter, gimbal, rid, risk, alert, capture, guardian, x, y, zone, source_node
```

## 十六、当前实现与未来扩展的边�?
### 当前已经可以直接使用的统丢�字段

- `node`
- `role`
- `ts`
- `track`
- `active`
- `confirmed`
- `x`
- `y`
- `vx`
- `vy`
- `seen`
- `lost`
- `hunter`
- `gimbal`
- `rid`
- `risk`
- `alert`
- `capture`
- `guardian`
- `reason`

### 当前只是建议预留的字�?
- `event_id`
- `event_level`
- `event_status`
- `zone`
- `source_node`
- `handover_from`
- `handover_to`
- `cluster_id`

## 十七、当前阶段结�?
从现在开始，`Node A` 的后续协议和材料建议按两层来维护�?
1. `A1 当前实现层`
   - �?[9NodeA上行字段表文�?md](C:/Users/WZwai/Documents/PlatformIO/Projects/Flytotal/docs/9NodeA正式字段表文�?md)

2. `未来统一接口层`
   - 看本文档

这样可以同时满足�?- 当前代码真实落地
- 后续系统整体统一扩展

