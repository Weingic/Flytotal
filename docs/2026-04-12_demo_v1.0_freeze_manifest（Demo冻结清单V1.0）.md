# Node A Hunter+Vision+Web Demo V1.0 冻结清单

冻结日期：`2026-04-12`  
冻结版本号：`Node A Hunter+Vision+Web Demo V1.0`

---

## 1. 冻结目标
本次冻结的目标不是继续加新功能，而是把当前已经跑通的单节点演示链路正式固化，形成一版可复现、可演示、可留档的稳定版本。

本次冻结覆盖以下四条链路：
1. `Node A` 主链状态链
2. 视觉增强链
3. 网页展示链
4. 事件证据链

---

## 2. 接线方式（冻结口径）
1. `Node A` 主控板通过 USB 连接电脑，当前使用串口口径：`COM4`。
2. 雷达接入 `Node A` 雷达串口链路，当前固件口径：`radar_baud=256000`。
3. 舵机接入 `Node A` 舵机控制口，当前运行状态口径：`servo_enabled=1`。
4. USB 摄像头直接接入电脑，由 `vision_bridge` 打开，不直接接入 `Node A`。
5. 舵机供电与主控地线保持共地。

---

## 3. 当前参数表口径
本次冻结引用以下参数基线文档：
1. [2026-04-09_single_node_tuning_table_v1（单节点联调参数表V1）.md](C:/Users/WZwai/Documents/PlatformIO/Projects/Flytotal/docs/2026-04-09_single_node_tuning_table_v1（单节点联调参数表V1）.md)
2. [2node_a_parameter_baseline（NodeA参数基线）.md](C:/Users/WZwai/Documents/PlatformIO/Projects/Flytotal/docs/2node_a_parameter_baseline（NodeA参数基线）.md)

本次冻结重点引用的运行参数口径如下：
1. 串口桥接波特率：`115200`
2. 雷达波特率：`256000`
3. 视觉跟踪器：`CSRT`
4. 跟踪回退策略：`auto`
5. 视觉源预热帧：`12`
6. 网页服务地址：`http://127.0.0.1:8765`
7. 事件进入口径延续 4.10 方案：`EventConfig::MissingRidEventMinDurationMs = 800`
8. 全量验收默认 closure 参数：`closure_timeout_s=4`、`closure_api_retries=4`

---

## 4. 冻结脚本范围
本次冻结以 `2026-04-12` 当天工作区版本为准，关键脚本如下：
1. `tools/preflight_411_环境预检.py`
2. `tools/startup_helper_411_单节点启动助手.py`
3. `tools/acceptance_flow_411_单节点闭环验收流程.py`
4. `tools/acceptance_auto_411_快检全检自动验收.py`
5. `tools/single_node_evidence_closure_check_单节点证据闭环核对.py`
6. `tools/node_a_serial_bridge_NodeA串口桥接.py`
7. `tools/vision_bridge_视觉桥接.py`
8. `tools/vision_web_server_视觉网页服务.py`
9. `tools/track_injector_轨迹注入器.py`
10. `tools/usb_camera_readiness_check_USB摄像头就绪核对.py`

---

## 5. 网页端版本范围
本次冻结的网页端版本由以下文件共同构成：
1. `tools/vision_dashboard.html`
2. `tools/vision_web_server_视觉网页服务.py`

当前网页端已具备：
1. 节点在线状态实时展示
2. 主状态 / Hunter 状态 / 云台状态 / RID 状态 / 风险等级展示
3. 最近事件列表展示
4. 事件详情（最小回放）展示
5. 抓拍图与抓拍路径展示
6. 事件证据 JSON 导出
7. 导出列表与导出详情回放

---

## 6. 已跑通标准场景
当前标准套件：`risk_event_vision_chain_v1`

已跑通场景：
1. `scenario1_short_missing_rid_no_direct_event`
2. `scenario2_sustained_missing_rid_risk_upgrade`
3. `scenario3_legal_target_keep_low_risk`
4. `scenario4_suspicious_rid_fast_risk_upgrade`
5. `scenario5_high_risk_visual_capture_ready`
6. `scenario6_track_lost_smooth_recover`

自动验收口径已跑通：
1. `quick_result=PASS`
2. `full_result=PASS`
3. `result=PASS`

---

## 7. 本次真机成功结果（2026-04-12）
本次固化日已取得以下真机联动结果：
1. 雷达主链进入正式跟踪，网页端显示 `云台 = TRACKING`。
2. 风险链成功提升，网页端显示 `SUSPICIOUS / HIGH_RISK` 等风险态。
3. 事件对象成功创建并展示事件号。
4. 视觉链完成手动选框后的锁定与抓拍。
5. 抓拍图成功通过 `event_id` 精确匹配挂接到当前事件。
6. 网页详情页可直接显示真实抓拍图、事件号、风险等级、RID 状态与坐标。

本次真机成功样例：
1. 事件号：`A1-0000002622-0001`
2. 抓拍绑定方式：`精确匹配（event_id）`
3. 详情来源：`实时链路`
4. 页面已展示真实抓拍画面、坐标与风险标签

---

## 8. 当前视觉模块做到哪一步
当前视觉模块已完成：
1. USB 摄像头就绪检测
2. 视觉桥接实时画面打开
3. 手动 ROI 选框
4. 选框后目标跟踪
5. 锁定后抓拍
6. 抓拍记录写入 `capture_records.csv`
7. 抓拍图与事件证据挂接

当前视觉模块尚未完成：
1. 自动目标检测驱动的无人值守起锁
2. 多目标视觉融合策略
3. 长时间无人值守连续稳定压测

当前冻结口径：
视觉模块属于“手动起锁 + 自动/手动抓拍”版本，而不是“全自动检测 + 自动起锁”版本。

---

## 9. 当前网页端做到哪一步
当前网页端已完成：
1. 主链状态实时展示
2. 最近事件列表展示
3. 事件详情最小回放展示
4. 抓拍图展示
5. 证据 JSON 导出
6. 导出列表与导出详情回放

当前网页端尚未完成：
1. 完整历史时间轴回放系统
2. 多轮会话聚合分析页
3. 更复杂的历史事件检索与筛选系统

当前冻结口径：
网页端已经不是单纯的状态看板，而是已经承担“事件留痕 + 证据展示 + 最小回放”的角色。

---

## 10. 当前已稳定的边界
1. 单节点 `Node A` 主链可以稳定输出状态并进入事件链。
2. 网页端可以稳定读取主链与视觉链数据并实时展示。
3. 事件可以在本地保留，并与抓拍证据通过 `event_id` 精确绑定。
4. 自动验收工具链可用于守门，快速判断当前链路是否可演示。
5. 当前版本已经具备一条可复现的单节点演示流程，可作为 `Demo V1.0` 冻结版本。

---

## 11. 当前未覆盖 / 未完全做稳的边界
1. 双节点协同链路未纳入本次冻结版本。
2. 视觉链仍需要人工选框，尚未进入自动检测阶段。
3. 长时间无人值守连续稳定性还没有形成系统压测结论。
4. 身份链 / 白名单链增强尚未并入本次冻结版本。
5. 网页端当前仍是最小回放，不是完整历史回放系统。

---

## 12. 固定验收命令
```powershell
python tools/acceptance_auto_411_快检全检自动验收.py --port COM4 --suite risk_event_vision_chain_v1 --base-url http://127.0.0.1:8765
```

通过口径：
1. `quick_result=PASS`
2. `full_result=PASS`
3. `result=PASS`

---

## 13. 冻结结论
截至 `2026-04-12`，当前版本已经满足以下冻结条件：
1. 一条可复现演示流程
2. 一版稳定参数口径
3. 一套统一字段
4. 一套网页端实时展示
5. 一条最小视觉融合链
6. 一组事件日志与抓拍证据
7. 一份可留档周报
8. 一个正式冻结的可演示版本

因此，本周冻结版本正式记为：`Node A Hunter+Vision+Web Demo V1.0`
